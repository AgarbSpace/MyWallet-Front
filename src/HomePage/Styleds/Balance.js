import styled from "styled-components";

const Balance = styled.span `
    font-family: Raleway;
    font-size: 17px;
    font-style: normal;
    font-weight: 700;
    line-height: 20px;
    letter-spacing: 0em;
    text-align: left;

    padding-left: 15px;

`

export default Balance;